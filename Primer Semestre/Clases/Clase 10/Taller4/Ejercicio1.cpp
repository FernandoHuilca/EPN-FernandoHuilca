// Author Fernando Huilca 
/* 1. Escriba un programa que calcule la suma de los numeros primos que existen entre un rango de valores.
 El programa le pregunta al usuario el limite inferior y el limite superior de este rango, luego presentara 
 el resultado. Se debe comprobar que los valores ingresados son enteros positivos y que el limite inferior 
 sea menoar al limete superor. El programa debe permitir realizar esta cálculo las veces que el usuario 
 quiera 
*/
#include <iostream>
using namespace std;

int main()
{
    int lim_superior, lim_inferior, resultado;
    cout << "PRIMOS\n";

    cout << "Escriba el límite superior: " << endl;
    cin >> lim_superior;
    cout << "Escriba el limite inferior: " << endl; 
    cin >> lim_inferior;

    // calculo de los primos 
    while (lim_inferior != lim_superior)
        {
            lim_superior --;
            for ()
            {
                if (resultado = 0)
                {                                                                                                                                                                                                                                                                                                                               
                    cout << "No es primo" << endl;
                }
                else 
                {
                    cout << "Si es primo " << endl; 
                }
            }

            
        }
        


}